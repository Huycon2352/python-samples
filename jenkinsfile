pipeline {

    agent any

    stages {

        stage('Checkout') {
            steps {
                git 'https://github.com/Huycon2352/python-samples/main'
            }
        }

        stage('Build') {
            steps {
                    sh 'docker build -t myapp .'
                }
            }
        }

    }

}
